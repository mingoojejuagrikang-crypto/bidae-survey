# Android Chrome 실기기 테스트 체크리스트

배포 주소: https://mingoojejuagrikang-crypto.github.io/bidae-survey/

---

## 1. 앱 접속 및 PWA 설치

- [ ] Chrome에서 위 URL 접속
- [ ] 주소창 오른쪽 "홈 화면에 추가" 아이콘 탭 (또는 메뉴 → 앱 설치)
- [ ] 홈 화면에 "비대조사" 아이콘 생성 확인
- [ ] 설치된 아이콘으로 재접속 시 독립 앱 창으로 열리는지 확인

---

## 2. 조사 행 생성

- [ ] 홈 화면 → "조사 행 생성" 탭
- [ ] "생성 예정 행 수: 90" 표시 확인
- [ ] "조사 행 생성" 버튼 탭
- [ ] "90행 생성 완료" 메시지 확인
- [ ] "입력 화면으로" 버튼 탭

---

## 3. 음성 입력 기본 동작

- [ ] "음성 시작" 버튼 탭 → 마이크 권한 허용
- [ ] 버튼이 "인식 중..."으로 바뀌는지 확인
- [ ] "삼십오점일" 또는 "35.1" 발화
- [ ] TTS가 "횡경 35.1"만 말하는지 확인 (다른 말 없이)
- [ ] 화면 횡경 값이 35.1로 업데이트되는지 확인

---

## 4. 연속 음성 모드 검증

- [ ] 음성 시작 후 자동 재시작되는지 확인 (발화 후 버튼을 다시 누르지 않아도 계속 인식)
- [ ] 발화와 발화 사이 3초 이상 침묵해도 인식이 유지되는지 확인
- [ ] "음성 종료" 버튼으로만 중지되는지 확인

---

## 5. 항목 이동

- [ ] 횡경 입력 후 "다음" 버튼 탭 → 현재 항목이 **종경**으로 이동하는지 확인
- [ ] 음성으로 "다음"이라고 말해서 이동하는지 확인
- [ ] 종경 입력 후 "다음" 탭 → 2번 행으로 이동하고 횡경으로 돌아오는지 확인
- [ ] "이전" 버튼 탭 → 이전 항목/행으로 이동하는지 확인
- [ ] 음성으로 "이전"이라고 말해서 이동하는지 확인
- [ ] 상단 표시 "1 / 90", "2 / 90" 등 행 번호 변화 확인

---

## 6. 수정 명령

- [ ] 횡경에 35.1 입력 후 음성으로 "수정 36.1" 발화
- [ ] 횡경 값이 36.1로 바뀌는지 확인 (TTS "횡경 36.1")
- [ ] 종경으로 이동한 후 "수정 42.0" 발화 → 종경이 42.0으로 수정되는지 확인

---

## 7. 경고 확인

- [ ] 범위를 벗어난 값(예: 200) 입력 시 경고 배지 표시 확인
- [ ] TTS가 "경고"를 말하는지 확인

---

## 8. 설정 화면

- [ ] 홈 → 설정 → "측정 입력 순서" 섹션 표시 확인 (횡경 → 종경)
- [ ] "순서 바꾸기" 버튼 탭 → 종경 → 횡경으로 변경되는지 확인
- [ ] 저장 후 입력 화면에서 종경이 먼저 나오는지 확인
- [ ] Apps Script URL 안내 문구 확인: "v0.1은 Apps Script 방식만 지원합니다"

---

## 9. 로그 ZIP 다운로드 및 내용 확인

- [ ] 입력 화면 → "로그 ZIP" 버튼 탭 → ZIP 파일 다운로드 확인
- [ ] ZIP 압축 해제 후 `logs.json` 파일 열기
- [ ] 다음 필드가 있는지 확인:
  - `recognizedText` (음성 인식 원문)
  - `normalizedValue` (정규화된 수치)
  - `finalValue` (최종 저장값)
  - `inputMode` ("voice" 또는 "manual")
  - `correctionType` ("normal", "수정", "이전", "다음", "manual" 중 하나)
  - `latencyMs` (응답 지연시간 ms)
- [ ] `audio/` 폴더에 `.webm` 또는 `.mp4` 실제 음성 파일이 있는지 확인
- [ ] `device.json`에 브라우저/OS 정보 확인

---

## 10. CSV 다운로드 컬럼 검증

- [ ] "CSV 다운" 버튼 탭 → CSV 파일 다운로드
- [ ] 열어서 다음 **10개** 컬럼만 있는지 확인 (로그/오디오 컬럼 없음):
  ```
  조사일자, 기준일자, 농가명, 라벨, 처리, 조사나무, 조사과실, 횡경, 종경, 비고
  ```

---

## 11. Apps Script 업로드 테스트

- [ ] 설정에서 Google Apps Script Web App URL 입력 후 저장
- [ ] 입력 화면 → "업로드" 버튼 탭
- [ ] "N행 업로드 완료" 메시지 확인
- [ ] 스프레드시트에 데이터가 추가됐는지 확인
- [ ] 업로드 URL 없이 업로드 시 "설정에서 Apps Script URL을 입력해 주세요" 안내 확인

---

## 12. 오프라인 동작 확인

- [ ] 데이터 입력 완료 후 Wi-Fi/모바일 데이터 끄기
- [ ] 앱 새로고침 또는 재접속
- [ ] 기존 입력 데이터가 그대로 보이는지 확인 (IndexedDB 유지)
- [ ] 오프라인 상태에서 입력 계속 가능한지 확인
- [ ] 다시 온라인으로 전환 후 업로드 가능한지 확인

---

## 체크리스트 완료 기준

모든 항목 통과 시 현장 배포 승인.
실패 항목은 GitHub Issues에 재현 단계 + 스크린샷 포함하여 등록.
